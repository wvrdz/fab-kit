Clean working tree. All acceptance criteria verified:

- **Created `fab/.kit/scripts/status.sh`** with exact content from ARCHITECTURE.md spec
- **Executable** (`chmod +x`, confirmed `-rwxr-xr-x`)
- **Uses `set -euo pipefail`**
- **No `fab/current`** → outputs "No active change", exits 0
- **`fab/current` pointing to missing folder** → outputs error message, exits 1
- **Valid change with branch** → outputs "Active: name (stage: X, branch: Y)"
- **Valid change without branch** → outputs "Active: name (stage: X)"
- **Relative path resolution** via `$(dirname "$0")/../../current`
- **Global quality gates pass**: core files exist, status.sh works

<promise>COMPLETE</promise>
