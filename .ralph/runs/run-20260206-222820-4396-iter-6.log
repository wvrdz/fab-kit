<promise>COMPLETE</promise>

**Summary**: Created `fab/.kit/skills/fab-new.md` (207 lines) — a comprehensive, agent-agnostic markdown skill prompt for the `/fab:new` command. The file covers all 14 acceptance criteria:

- References `_context.md` preamble at the top
- Accepts `<description>` and optional `--branch <name>` arguments
- Generates folder name in `{YYMMDD}-{XXXX}-{slug}` format with slug extraction rules
- Creates `fab/changes/{name}/` directory and writes to `fab/current`
- Git integration with create branch/adopt current/skip options (and `--branch` override)
- Initializes `.status.yaml` with `stage: proposal`, `proposal: active`, all others `pending`
- Generates `proposal.md` from template with constitution and config context
- Clarifying questions (max 3 `[BLOCKING]`)
- Marks proposal complete when user is satisfied
- Ends with `Next: /fab:continue or /fab:ff`
- Example shows `260206-x7k2-add-oauth/` naming
- Negative case: aborts with `/fab:init` guidance if `config.yaml` missing

Commits: `dfa7adc` (feat) + `bb7b3d7` (progress). Working tree clean.
