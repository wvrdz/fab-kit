# Migration: 0.44.0 to 0.45.0

## Summary

Clean up `.envrc`: fix `fab-kit sync` → `fab sync`, and remove the legacy `PATH_add fab/.kit/scripts` line. The `fab` router is the correct entry point; `fab-kit sync` bypasses the router. Scripts in `fab/.kit/scripts/` are now invoked via `fab` subcommands, not direct PATH.

## Pre-check

1. Confirm `.envrc` exists in the repo root — if not, skip this migration entirely

## Changes

### 1. Fix WORKTREE_INIT_SCRIPT in `.envrc`

If `.envrc` contains `fab-kit sync`:

1. Replace all occurrences of `fab-kit sync` with `fab sync`
2. Print: `Fixed: .envrc — replaced 'fab-kit sync' with 'fab sync'`

If `.envrc` does not contain `fab-kit sync`, print: `.envrc already correct, skipping.`

### 2. Remove `PATH_add fab/.kit/scripts` from `.envrc`

If `.envrc` contains a line matching `PATH_add fab/.kit/scripts`:

1. Remove that line from `.envrc`
2. Print: `Removed 'PATH_add fab/.kit/scripts' from .envrc (scripts are now invoked via fab subcommands).`

If `.envrc` does not contain that line, print: `.envrc already clean, skipping.`

## Verification

1. `.envrc` does NOT contain `fab-kit sync`
2. `.envrc` contains `fab sync` in the `WORKTREE_INIT_SCRIPT` line
3. `.envrc` does NOT contain `PATH_add fab/.kit/scripts`
