# Migration: 0.45.1 to 0.46.0

## Summary

Remove orphaned `fab/.kit-sync-version` file. This stamp file is no longer written or read — sync staleness detection now compares `fab/.kit/VERSION` against `fab_version` in `fab/project/config.yaml`.

## Pre-check

1. Confirm the project has a `fab/` directory — if not, skip this migration entirely

## Changes

### 1. Remove `fab/.kit-sync-version` if present

If the file `fab/.kit-sync-version` exists:

1. Delete `fab/.kit-sync-version`
2. Print: `Removed: fab/.kit-sync-version (obsolete sync stamp — staleness detection now uses config.yaml fab_version)`

If the file does not exist, print: `fab/.kit-sync-version already absent, skipping.`

## Verification

1. `fab/.kit-sync-version` does NOT exist
