# Quality Checklist: Operator tick-start and time subcommands for fab-go

**Change**: 260405-px5o-operator-tick-start-time-subcommands
**Generated**: 2026-04-05
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-continue or /fab-ff at the tasks stage.

  This checklist validates that the IMPLEMENTATION matches the SPEC.
  Items are derived from:
    1. spec.md — every requirement should have coverage
    2. Plan decisions — technical choices should be reflected in code
    3. Project constitution — project-wide quality standards

  Categories below are defaults. Add project-specific categories from config.yaml.
  /fab-continue (review) checks all items. ALL must pass before archiving.
-->

## Functional Completeness
<!-- Every requirement in spec.md has working implementation -->
- [x] CHK-001 Operator parent command refactor: `operatorCmd()` in `src/go/fab/cmd/fab/operator.go` no longer has `Args: cobra.NoArgs`; it registers `tick-start` and `time` as subcommands via `cmd.AddCommand(...)`
- [x] CHK-002 Operator default RunE preserved: `RunE: runOperator` is retained on the parent command; invoking `fab operator` with no subcommand still fires `runOperator` unchanged
- [x] CHK-003 tick-start reads `.fab-operator.yaml`: subcommand resolves repo root via `gitRepoRoot()` and reads the file into `map[string]interface{}` using `gopkg.in/yaml.v3`
- [x] CHK-004 tick-start missing-file handling: absent `.fab-operator.yaml` is treated as an empty document (`tick_count` = 0); no error is returned
- [x] CHK-005 tick-start increments tick_count: `tick_count` is incremented by 1 (from 0 if absent); result written back to `.fab-operator.yaml`
- [x] CHK-006 tick-start writes last_tick_at: `last_tick_at` is set to `time.Now().UTC().Format(time.RFC3339)` (RFC3339 UTC string, e.g. `2026-04-05T10:42:00Z`)
- [x] CHK-007 tick-start stdout format: output is exactly `tick: N\nnow: HH:MM` (two lines, local 24-hour time via `time.Now().Format("15:04")`)
- [x] CHK-008 tick-start has no flags: the subcommand accepts no flags
- [x] CHK-009 tick-start preserves all other fields: fields such as `monitored`, `autopilot_queue`, `branch_map` are not altered when writing back the updated map
- [x] CHK-010 tick-start write failure exits 1: write errors print to stderr and exit with code 1
- [x] CHK-011 time outputs now unconditionally: `fab operator time` always outputs `now: HH:MM` using `time.Now().Format("15:04")`
- [x] CHK-012 time --interval flag defined: `--interval` flag (string type, no default) is registered on the `time` subcommand
- [x] CHK-013 time with --interval outputs next: when `--interval` is provided, a second line `next: HH:MM` is output using `time.Now().Add(parsedDuration).Format("15:04")`
- [x] CHK-014 time invalid interval exits 1: invalid Go duration string prints error to stderr and exits 1
- [x] CHK-015 time has no file I/O: the `time` subcommand reads no files and writes no files under any code path
- [x] CHK-016 Tests added to operator_test.go: all five new test functions (`TestOperatorTickStart_IncrementsCount`, `TestOperatorTickStart_MissingFile`, `TestOperatorTime_NoInterval`, `TestOperatorTime_WithInterval`, `TestOperatorTime_InvalidInterval`) exist in `src/go/fab/cmd/fab/operator_test.go`
- [x] CHK-017 TestOperatorCmd_Structure updated: existing test asserts that the operator command has two registered subcommands named `tick-start` and `time` (verified via `cmd.Commands()`)
- [x] CHK-018 fab-operator.md step 1 updated: Tick Behavior step 1 instructs the agent to run `fab operator tick-start` and parse stdout for `tick: N` and `now: HH:MM` (manual increment instruction removed)
- [x] CHK-019 fab-operator.md idle message updated: Idle Message paragraph instructs use of `fab operator time --interval {interval}` to obtain `now:` and `next:` values; example idle message line preserved
- [x] CHK-020 _cli-fab.md tick-start section added: `### fab operator tick-start` subsection documents usage, output format, side effects on `.fab-operator.yaml`, and no-flags constraint
- [x] CHK-021 _cli-fab.md time section added: `### fab operator time` subsection documents usage, `--interval` flag, both output formats (with/without flag), and invalid-duration error behavior
- [x] CHK-022 _cli-fab.md existing fab operator prose preserved: all pre-existing `## fab operator` prose is unchanged
- [x] CHK-023 execution-skills.md updated: operator section documents both `fab operator tick-start` and `fab operator time` with purpose, output format, side effects, and their roles in tick step 1 and idle message
- [x] CHK-024 kit-architecture.md updated: `fab operator` Command Reference entry notes the two new subcommands (`tick-start`, `time`) and their purposes

## Behavioral Correctness
<!-- Changed requirements behave as specified, not as before -->
- [x] CHK-025 fab operator launch behavior unchanged: running `fab operator` (no subcommand) inside tmux produces identical behavior to pre-refactor — creates or selects "operator" window; no behavioral regression
- [x] CHK-026 tick-start uses Go time package (not shell date): all clock operations use `time.Now()` from the Go `time` package; no shell subprocess or `exec.Command("date", ...)` calls
- [x] CHK-027 tick-start output uses local time: `now: HH:MM` reflects the operator's local timezone (24-hour), not UTC
- [x] CHK-028 last_tick_at uses UTC: `last_tick_at` is stored as UTC (not local time), formatted with `time.RFC3339`

## Removal Verification
<!-- Every deprecated requirement is actually gone -->
- [x] CHK-029 cobra.NoArgs removed: `Args: cobra.NoArgs` does not appear on the operator parent command in `operator.go`; subcommand dispatch works correctly

## Scenario Coverage
<!-- Key scenarios from spec.md have been exercised -->
- [x] CHK-030 Scenario — existing operator launch still works: `TestOperatorCmd_Structure` or an integration check confirms `fab operator` with no subcommand invokes `runOperator`
- [x] CHK-031 Scenario — subcommands discoverable via help: `fab operator --help` output includes `tick-start` and `time` in the available commands list
- [x] CHK-032 Scenario — tick-start increments existing count: `TestOperatorTickStart_IncrementsCount` creates `.fab-operator.yaml` with `tick_count: 5`, runs tick-start, verifies stdout `tick: 6` and written YAML `tick_count: 6`
- [x] CHK-033 Scenario — tick-start creates missing file: `TestOperatorTickStart_MissingFile` verifies stdout `tick: 1` and created file with `tick_count: 1` and `last_tick_at` set
- [x] CHK-034 Scenario — tick-start preserves extra fields: test (T004) writes a YAML with extra field (e.g. `monitored: {}`), runs tick-start, reads back and confirms extra field is unchanged
- [x] CHK-035 Scenario — time without --interval: `TestOperatorTime_NoInterval` verifies exactly one line matching `now: \d\d:\d\d` and absence of `next:` line
- [x] CHK-036 Scenario — time with --interval: `TestOperatorTime_WithInterval` runs with `--interval 3m`, verifies both `now: \d\d:\d\d` and `next: \d\d:\d\d` lines present
- [ ] CHK-037 Scenario — time invalid interval: `TestOperatorTime_InvalidInterval` does NOT invoke `cmd.Execute()` with the invalid interval — it only calls `time.ParseDuration("notaduration")` directly and checks flag registration. The full end-to-end path (parse error → stderr write → os.Exit(1)) is not exercised by the test. The spec requires verifying "exit 1 and stderr error."
- [x] CHK-038 Scenario — agent uses tick-start at step 1: `fab-operator.md` step 1 text instructs invocation of `fab operator tick-start`; no `shell date` or manual YAML write instruction remains
- [x] CHK-039 Scenario — agent computes idle message correctly: `fab-operator.md` idle message section shows `fab operator time --interval {interval}` producing `now:` and `next:` that map directly to the idle message template

## Edge Cases & Error Handling
<!-- Error states, boundary conditions, failure modes -->
- [x] CHK-040 tick-start write failure: when `.fab-operator.yaml` cannot be written (e.g. read-only directory), error goes to stderr and process exits 1; no panic
- [x] CHK-041 tick-start absent tick_count field: YAML file exists but has no `tick_count` key — treated as 0, result is `tick_count: 1` written
- [x] CHK-042 tick-start empty YAML file: completely empty `.fab-operator.yaml` is treated as an empty document (not a parse error); `tick_count` becomes 1
- [x] CHK-043 time duration edge: zero duration (`--interval 0s`) is a valid Go duration — `next:` equals `now:`; no crash
- [x] CHK-044 time no --interval: the `next:` line is completely absent from stdout (not an empty line); single-line output only
- [x] CHK-045 Tests use t.TempDir(): tick-start tests (T004, T005) isolate all `.fab-operator.yaml` I/O to a `t.TempDir()`; no real repo root is accessed during tests

## Code Quality
<!-- Always included. Baseline items when no code_quality config; expanded when config exists -->
- [x] CHK-046 Pattern consistency: new `tick-start` and `time` subcommand functions follow the naming and structural patterns of existing cobra command functions in `operator.go` (e.g. function naming, error returns, stdout writers)
- [x] CHK-047 No unnecessary duplication: existing utilities (`gitRepoRoot()`, yaml marshal/unmarshal patterns) are reused rather than reinvented in the new subcommands
- [x] CHK-048 Readability: new subcommand functions are concise and clearly structured; no god functions (each subcommand handler stays under ~50 lines)
- [ ] CHK-049 No magic strings: `"tick_count"` and `"last_tick_at"` appear as bare string literals in `operator_tick_start.go` (lines 53, 63, 64) without named constants. `"15:04"` similarly appears as a bare format literal. The spec and code-quality.md flag this as an anti-pattern.
- [x] CHK-050 No god functions: `runTickStart` and `runTime` handlers each have a single clear responsibility and do not exceed 50 lines without justification

## Documentation Accuracy
<!-- config.yaml extra_categories: documentation_accuracy -->
- [x] CHK-051 fab-operator.md step 1 exact text: the updated step 1 reads approximately "run `fab operator tick-start` (increments `tick_count`, writes `last_tick_at`, outputs `tick: N` and `now: HH:MM`). Parse stdout for the tick number and current time. Then run `fab pane map` and read `.fab-operator.yaml`." as specified in spec
- [x] CHK-052 fab-operator.md idle message example: the example `Waiting for next tick. Time: 08:26 · next tick: 08:29` is retained in the updated idle message paragraph
- [x] CHK-053 _cli-fab.md output format accuracy: documented output blocks in `### fab operator tick-start` and `### fab operator time` exactly match the implementation's stdout format (`tick: N`, `now: HH:MM`, `next: HH:MM`)
- [x] CHK-054 execution-skills.md side-effect accuracy: documentation correctly distinguishes `tick-start` (has side effects on `.fab-operator.yaml`) from `time` (pure query, no side effects)
- [x] CHK-055 kit-architecture.md command reference accuracy: the `fab operator` entry correctly reflects the new parent-with-subcommands structure; no stale description of it as a leaf command

## Cross References
<!-- config.yaml extra_categories: cross_references -->
- [x] **N/A** CHK-056 _cli-fab.md updated per constitution: no `docs/specs/skills/SPEC-_cli-fab.md` file exists in the repo — the constitution's "MUST update corresponding SPEC-*.md" constraint does not apply to `_cli-fab.md` (no SPEC file to update)
- [x] **N/A** CHK-057 fab-operator.md spec file: `docs/specs/skills/SPEC-fab-operator.md` does not exist — only `SPEC-fab-operator4.md` exists (for the retired `fab-operator4.md` skill). No corresponding SPEC file to update for the current `fab-operator.md`.
- [x] CHK-058 _cli-fab.md cross-reference consistency: the new `### fab operator tick-start` and `### fab operator time` sections in `_cli-fab.md` are consistent with the corresponding operator.go implementation and with `fab-operator.md`'s usage instructions
- [x] CHK-059 execution-skills.md ↔ kit-architecture.md consistency: the operator subcommand descriptions in both memory files are consistent with each other and with the spec's output format definitions
- [x] CHK-060 Constitution CLI constraint satisfied: constitution states "Changes to the `fab` CLI MUST update `src/kit/skills/_cli-fab.md` with any new or changed command signatures" — confirm `_cli-fab.md` documents both `fab operator tick-start` and `fab operator time`

## Notes

- Check items as you review: `- [x]`
- All items must pass before `/fab-continue` (hydrate)
- If an item is not applicable, mark checked and prefix with **N/A**: `- [x] CHK-056 **N/A**: {reason}`
