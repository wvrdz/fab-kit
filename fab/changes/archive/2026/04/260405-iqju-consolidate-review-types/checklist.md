# Quality Checklist: Consolidate Review Types

**Change**: 260405-iqju-consolidate-review-types
**Generated**: 2026-04-05
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-continue or /fab-ff at the tasks stage.

  This checklist validates that the IMPLEMENTATION matches the SPEC.
  Items are derived from:
    1. spec.md — every requirement should have coverage
    2. Plan decisions — technical choices should be reflected in code
    3. Project constitution — project-wide quality standards

  Categories below are defaults. Add project-specific categories from config.yaml.
  /fab-continue (review) checks all items. ALL must pass before archiving.
-->

## Functional Completeness

<!-- Every requirement in spec.md has working implementation -->

- [ ] CHK-001 `_review.md` file created: `src/kit/skills/_review.md` exists with correct frontmatter (`user-invocable: false`, `disable-model-invocation: true`, `metadata.internal: true`)
- [ ] CHK-002 Inward sub-agent defined in `_review.md`: all six validation checks (tasks complete, quality checklist, run affected tests, spot-check spec, memory drift check, code quality check) are present
- [ ] CHK-003 Outward sub-agent defined in `_review.md`: dispatched via Agent tool (`general-purpose`), receives git diff + changed file list + standard subagent context, has full tool access, uses Codex→Claude cascade
- [ ] CHK-004 Parallel dispatch documented in `_review.md`: both sub-agents explicitly dispatched in parallel, findings merged before verdict
- [ ] CHK-005 Merged findings feed single verdict in `_review.md`: any must-fix from either sub-agent triggers rework; pass only if merged set has no must-fix
- [ ] CHK-006 Outward sub-agent always-on: no config flag check anywhere in `_review.md`; cascade gracefully no-ops if both Codex and Claude are unavailable
- [ ] CHK-007 `fab-continue.md` delegates to `_review.md`: Stage dispatch table has `review` row referencing `_review.md`; `## Review Behavior` section replaced with delegation reference
- [ ] CHK-008 `fab-continue.md` Verdict logic retained: pass/fail state transitions, rework options table, and `fab status` calls remain in `fab-continue.md` (not moved to `_review.md`)
- [ ] CHK-009 `git-pr-review.md` Phase 2 is Copilot-only: Codex and Claude tool invocations removed from Phase 2 entirely
- [ ] CHK-010 `git-pr-review.md` poll-and-wait implemented: after successful `gh pr edit --add-reviewer copilot`, polls every 30s up to 20 attempts (10 min) for Copilot review to appear
- [ ] CHK-011 `git-pr-review.md` clean finish on timeout: prints `Copilot review requested but not yet available. Re-run /git-pr-review to process when ready.` after 10 min; no error/fail status
- [ ] CHK-012 `git-pr-review.md` clean finish on Copilot failure: prints `No automated reviewer available. Run /git-pr-review when reviews are added.` on non-zero exit from `gh pr edit`; no error/fail status
- [ ] CHK-013 `git-pr-review.md` Step 2a and Step 2b removed: no context enrichment, no local review output posting in Phase 2
- [ ] CHK-014 `git-pr-review.md` `--tool` flag updated: valid values reduced to `copilot` only; error message for invalid tools updated to `Invalid tool: {name}. Valid values: copilot.`
- [ ] CHK-015 `git-pr-review.md` `review_tools` config block updated: shows `copilot: true` only (no `codex`, no `claude` rows)
- [ ] CHK-016 `fab-ff.md` Step 6 updated: references `_review.md` as authoritative source for inward + outward sub-agent dispatch
- [ ] CHK-017 `fab-fff.md` Step 6 updated: references `_review.md` as authoritative source for inward + outward sub-agent dispatch
- [ ] CHK-018 `fab/project/config.yaml` updated: `review_tools` block contains only `copilot: true` (no `codex`, no `claude` keys)
- [ ] CHK-019 Migration file created: `src/kit/migrations/1.3.0-to-1.4.0.md` exists with correct pre-check, `yq del` commands, and verification steps

## Behavioral Correctness

<!-- Changed requirements behave as specified, not as before -->

- [ ] CHK-020 Inward sub-agent behavior unchanged: content extracted to `_review.md` is identical to what was in `fab-continue.md` — no behavioral drift
- [ ] CHK-021 Outward sub-agent prompt instructs correct focus areas: interface contract violations, memory pattern inconsistencies, missing cross-references, behavioral regressions requiring full-repo context, structural issues (duplication of utilities)
- [ ] CHK-022 Codex→Claude cascade graceful no-op: when neither tool is available, outward sub-agent returns empty findings (no must-fix/should-fix/nice-to-have); review continues normally
- [ ] CHK-023 Copilot disabled in config skips Phase 2 cleanly: when `review_tools.copilot: false`, Phase 2 prints `No automated reviewer available. Run /git-pr-review when reviews are added.` without attempting the `gh pr edit` command
- [ ] CHK-024 Migration is idempotent: running the migration twice does not produce an error or modify the file a second time (yq del on absent keys is a no-op)
- [ ] CHK-025 `git-pr-review.md` Phase 1 (existing human/bot review processing) is completely unchanged

## Removal Verification

<!-- Every deprecated requirement is actually gone -->

- [ ] CHK-026 Codex invocation removed from `git-pr-review.md`: no `codex review` command, no `command -v codex` check, no Tool 2 block in Phase 2
- [ ] CHK-027 Claude one-shot invocation removed from `git-pr-review.md`: no `claude -p "Review this PR diff..."` call, no `command -v claude` check, no Tool 3 block in Phase 2
- [ ] CHK-028 `--tool codex` and `--tool claude` options removed from `git-pr-review.md`: Step 1.5 validation rejects `codex` and `claude` as invalid values
- [ ] CHK-029 `review_tools.codex` and `review_tools.claude` keys absent from `fab/project/config.yaml`
- [ ] CHK-030 `Bash(codex:*)` and `Bash(claude:*)` removed from `git-pr-review.md` `allowed-tools` frontmatter
- [ ] CHK-031 Inline sub-agent dispatch logic removed from `fab-continue.md` Review Behavior: no duplicate Validation Steps, Sub-Agent Dispatch, or Structured Review Output sections remain in `fab-continue.md`

## Scenario Coverage

<!-- Key scenarios from spec.md have been exercised -->

- [ ] CHK-032 Scenario: File structure mirrors `_generation.md` — `_review.md` frontmatter matches required structure; opening block-quote cites orchestrators
- [ ] CHK-033 Scenario: Must-fix from outward sub-agent triggers rework — merged findings logic in `_review.md` (or orchestrator) uses combined set for pass/fail determination
- [ ] CHK-034 Scenario: No must-fix from either — review passes — both sub-agents reporting only should-fix/nice-to-have does not trigger rework
- [ ] CHK-035 Scenario: `--tool copilot` still valid — Step 1.5 accepts `copilot`; routes to Copilot-only path
- [ ] CHK-036 Scenario: `--tool codex` now invalid — Step 1.5 rejects `codex` with `Invalid tool: codex. Valid values: copilot.`
- [ ] CHK-037 Scenario: Copilot review appears during poll — polling logic stops and falls through to Step 3 when `gh pr view --json reviews` returns a Copilot review
- [ ] CHK-038 Scenario: No `review_tools` block — defaults to Copilot enabled (existing default behavior preserved)
- [ ] CHK-039 Scenario: Migration removes both keys — `review_tools.codex` and `review_tools.claude` deleted; `review_tools.copilot` retains its value
- [ ] CHK-040 Scenario: No `review_tools` block in config — migration prints skip message and exits without modifying the file
- [ ] CHK-041 Scenario: `fab-continue` dispatches review via `_review.md` reference — `fab-continue.md` Review Behavior section contains the delegation line referencing `_review.md`

## Edge Cases & Error Handling

<!-- Error states, boundary conditions, failure modes -->

- [ ] CHK-042 Both Codex and Claude unavailable (no CLIs) — outward sub-agent gracefully returns empty findings; no error is raised; review stage continues
- [ ] CHK-043 Copilot request succeeds but no review within 10 min — timeout message printed, stage completes as clean finish (not fail, no error event)
- [ ] CHK-044 Copilot request fails (non-zero exit) — clean finish message printed, stage completes cleanly (not error)
- [ ] CHK-045 Migration run on config with no `review_tools` block — migration skips gracefully, does not modify the file
- [ ] CHK-046 Migration run on config already migrated (keys absent) — yq del is a no-op, no error, no duplicate output
- [ ] CHK-047 `_review.md` `disable-model-invocation: true` prevents direct user invocation — file is internal-only, not user-facing

## Code Quality

<!-- Baseline items + project-specific principles from code-quality.md -->

- [ ] CHK-048 Pattern consistency: `_review.md` follows the same structural pattern as `_generation.md` (frontmatter shape, opening block-quote, section naming)
- [ ] CHK-049 No unnecessary duplication: inward sub-agent content extracted verbatim from `fab-continue.md`; no independent copy remains in `fab-continue.md`
- [ ] CHK-050 Readability and maintainability: `_review.md` sections are clearly named and self-contained; a reader unfamiliar with the change can understand the dispatch behavior without cross-referencing `fab-continue.md`
- [ ] CHK-051 Follow existing project patterns: delegation pattern in `fab-continue.md` matches exactly how `_generation.md` is referenced (same table row format, same reference style)
- [ ] CHK-052 Prefer composition over inheritance: orchestrators reference `_review.md` by name rather than inlining review logic
- [ ] CHK-053 No god functions: outward sub-agent definition in `_review.md` is appropriately scoped — focuses on dispatch instructions and cascade logic, does not embed unrelated orchestration
- [ ] CHK-054 No duplicating existing utilities: migration uses `yq del` as specified, consistent with other migration files in `src/kit/migrations/`

## Documentation Accuracy

<!-- Extra category from config.yaml checklist.extra_categories -->

- [ ] CHK-055 `SPEC-fab-continue.md` updated: sub-agents table shows `_review` row; Review stage description references `_review.md`
- [ ] CHK-056 `SPEC-git-pr-review.md` updated: Phase 2 flow shows Copilot-only; `review_tools` config example has `copilot` key only; Step 2a and 2b absent; `--tool` valid values updated
- [ ] CHK-057 `SPEC-fab-ff.md` updated: Step 6 (Review) references `_review.md` as authoritative source
- [ ] CHK-058 `SPEC-fab-fff.md` updated: Step 6 (Review) references `_review.md` as authoritative source
- [ ] CHK-059 Skill description in `git-pr-review.md` frontmatter reflects Copilot-only automated review (no longer mentions Codex or Claude cascade)

## Cross References

<!-- Extra category from config.yaml checklist.extra_categories -->

- [ ] CHK-060 `fab-ff.md` Step 6 cross-reference to `_review.md` is accurate and points to the correct file
- [ ] CHK-061 `fab-fff.md` Step 6 cross-reference to `_review.md` is accurate and points to the correct file
- [ ] CHK-062 `fab-continue.md` stage dispatch table cross-reference `| review | Review Behavior (_review.md) |` matches the actual file name `_review.md`
- [ ] CHK-063 Migration file `1.3.0-to-1.4.0.md` accurately references `fab/project/config.yaml` (correct path for user projects)

## Notes

- Check items as you review: `- [x]`
- All items must pass before `/fab-continue` (hydrate)
- If an item is not applicable, mark checked and prefix with **N/A**: `- [x] CHK-001 **N/A**: {reason}`
