# Quality Checklist: Simplify Stages and Entry Paths

**Change**: 260212-v5p2-simplify-stages-entry-paths
**Generated**: 2026-02-12
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-continue at the tasks stage.

  This checklist validates that the IMPLEMENTATION matches the SPEC.
  /fab-review checks all items. ALL must pass before /fab-archive.
-->

## Functional Completeness

<!-- Every requirement in spec.md has working implementation -->
- [x] CHK-001 Remove Brief as Pipeline Stage: `.status.yaml` template and `config.yaml` contain no `brief` stage; pipeline is 5 stages
- [x] CHK-002 Update config.yaml Stages: `spec` has no `requires` field; `tasks` requires `[spec]`; no `brief` entry exists
- [x] CHK-003 Remove `stage:` Field: `.status.yaml` template has no `stage:` field; progress map `active` entry is single source of truth
- [x] CHK-004 State Vocabulary: progress map uses exactly `pending`, `active`, `done`, `failed` — no other values
- [x] CHK-005 Preflight Derives Stage: `fab-preflight.sh` outputs `stage:` derived from `active` entry; no `brief:` in output
- [x] CHK-006 SRAD-Driven Question Count: `fab-new.md` uses SRAD scoring for question count, no fixed cap
- [x] CHK-007 Conversational Mode: `fab-new.md` enters one-at-a-time questioning when Signal Strength is low
- [x] CHK-008 Gap Analysis: `fab-new.md` performs gap analysis before folder creation
- [x] CHK-009 Brief-Only Output: `fab-new.md` produces only `brief.md`, never `spec.md`
- [x] CHK-010 Origin Section: brief template includes `## Origin` section; `fab-new.md` populates it with raw user input
- [x] CHK-011 Delete fab-discuss: `fab-discuss.md` skill file deleted; all symlinks removed
- [x] CHK-012 Remove fab-discuss References: no `/fab-discuss` references remain in docs or skills (except changelogs)
- [x] CHK-013 Next Steps Tables: `/fab-init` and `/fab-hydrate` next lines exclude `/fab-discuss`; `_context.md` table has no discuss rows
- [x] CHK-014 Clarify Brief Refinement: `fab-clarify.md` scans `brief.md` with brief taxonomy at spec stage
- [x] CHK-015 Clarify Stage Guard: `fab-clarify.md` accepts `spec` and `tasks` only; no `brief` in valid list
- [x] CHK-016 fab-continue Active Logic: `fab-continue.md` uses `active` entry lookup, not `stage:` field
- [x] CHK-017 fab-ff Pipeline: `fab-ff.md` runs 2-stage pipeline (spec → tasks), not 3
- [x] CHK-018 fab-fff Gate: `fab-fff.md` checks `brief.md` exists, not brief stage done
- [x] CHK-019 fab-status Display: `fab-status.sh` shows 5 stages (x/5), no brief line
- [x] CHK-020 fab-switch Numbering: `fab-switch.md` uses spec=1 through archive=5
- [x] CHK-021 Shell Scripts Updated: `fab-help.sh` says "spec → tasks"; all three scripts reference 5-stage model
- [x] CHK-022 Migration Note: documentation includes manual migration steps for old-format `.status.yaml`

## Behavioral Correctness

<!-- Changed requirements behave as specified, not as before -->
- [x] CHK-023 Two-Write Transitions: stage completion sets current → `done` and next → `active` in one operation
- [x] CHK-024 Review Failure Backward Movement: `/fab-review` sets `review: failed` AND `apply: active` automatically
- [x] CHK-025 Reset-to-Brief Rejection: `/fab-continue brief` outputs "Cannot reset to brief — brief is not a pipeline stage."
- [x] CHK-026 Exactly-One-Active Invariant: at most one `active` entry in progress map at any time (all `done` = completed)

## Removal Verification

<!-- Every deprecated requirement is actually gone -->
- [x] CHK-027 fab-discuss Skill Deleted: `fab/.kit/skills/fab-discuss.md` does not exist
- [x] CHK-028 fab-discuss Symlinks Deleted: `.claude/skills/fab-discuss/`, `.opencode/commands/fab-discuss.md`, `.agents/skills/fab-discuss/` do not exist
- [x] CHK-029 No Brief Stage in Pipeline: no skill or script treats brief as a pipeline stage
- [x] CHK-030 No `stage:` in Template: `fab/.kit/templates/status.yaml` has no `stage:` field
- [x] CHK-031 No Fixed Question Cap: `fab-new.md` has no hardcoded "max 3 questions" logic

## Scenario Coverage

<!-- Key scenarios from spec.md have been exercised -->
- [x] CHK-032 New change after /fab-new: `.status.yaml` has `spec: active`, no `brief:`, no `stage:`
- [x] CHK-033 Preflight with no `stage:` field: script derives stage correctly from progress map
- [x] CHK-034 fab-continue from `spec: active`: generates spec, transitions to `{spec: done, tasks: active}`
- [x] CHK-035 Reset to spec: sets `{spec: active, tasks: pending, apply: pending, ...}`
- [x] CHK-036 fab-clarify at spec stage: scans both brief.md and spec.md with per-artifact taxonomy
- [x] CHK-037 fab-ff from new change: generates spec → auto-clarify → tasks → auto-clarify, lands at `apply: active`

## Edge Cases & Error Handling

<!-- Error states, boundary conditions, failure modes -->
- [x] CHK-038 All stages done: skills recognize completed change (no `active` entry)
- [x] CHK-039 Preflight with all done: outputs `stage: archive` (last done entry)
- [x] CHK-040 fab-setup.sh after discuss deletion: symlink creation succeeds without fab-discuss.md

## Notes

- Check items as you review: `- [x]`
- All items must pass before `/fab-archive`
- If an item is not applicable, mark checked and prefix with **N/A**: `- [x] CHK-008 **N/A**: {reason}`
