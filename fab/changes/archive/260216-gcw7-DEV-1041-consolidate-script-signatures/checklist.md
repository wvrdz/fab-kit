# Quality Checklist: Consolidate Script Signatures

**Change**: 260216-gcw7-DEV-1041-consolidate-script-signatures
**Generated**: 2026-02-16
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-ff at the tasks stage.

  This checklist validates that the IMPLEMENTATION matches the SPEC.
  Items are derived from:
    1. spec.md — every requirement should have coverage
    2. Plan decisions — technical choices should be reflected in code
    3. Project constitution — project-wide quality standards

  Categories below are defaults. Add project-specific categories from config.yaml.
  /fab-continue (review) checks all items. ALL must pass before archiving.
-->

## Functional Completeness
<!-- Every requirement in spec.md has working implementation -->
- [x] CHK-001 Dead function removal: All 12 dead functions are deleted from `stageman.sh` (no function definitions remain)
- [x] CHK-002 Internal-only CLI dispatch removal: All 6 internal-only case arms removed from dispatch block; corresponding functions still exist and are callable internally
- [x] CHK-003 Self-test removal: `run_tests()`, `show_version()`, `--test`, `--version` dispatch entries are all deleted
- [x] CHK-004 Retained subcommand set: CLI dispatch contains exactly 14 subcommands + `--help` + empty-arg→help
- [x] CHK-005 Help text updated: `show_help()` lists only retained subcommands in correct categories; no removed subcommand names appear
- [x] CHK-006 `_apply_metrics_side_effect` preserved: Function unchanged, called by `set_stage_state` and `transition_stages`

## Behavioral Correctness
<!-- Changed requirements behave as specified, not as before -->
- [x] CHK-007 Empty-arg behavior changed: `stageman.sh` (no args) now shows help instead of running tests
- [x] CHK-008 Unknown subcommand error: `stageman.sh stage-number spec` returns "Unknown option: stage-number" to stderr + exit 1

## Removal Verification
<!-- Every deprecated requirement is actually gone -->
- [x] CHK-009 No dead function remnants: grep for each of the 12 removed function names confirms zero definitions in `stageman.sh`
- [x] CHK-010 No dead CLI dispatch remnants: grep for each of the 18 removed case arms confirms zero in dispatch block
- [x] CHK-011 No `--test`/`--version` remnants: grep confirms no `--test`, `--version`, `run_tests`, `show_version` in `stageman.sh`

## Scenario Coverage
<!-- Key scenarios from spec.md have been exercised -->
- [x] CHK-012 Retained subcommand works unchanged: `stageman.sh all-stages` returns 6 stages; `stageman.sh progress-map <file>` returns correct pairs
- [x] CHK-013 Removed subcommand returns error: Removed subcommand (e.g., `stage-number`) exits 1 with error message
- [x] CHK-014 Metrics side-effects after removal: `set-state ... active` still writes `started_at`, `driver`, `iterations` to `stage_metrics` (verified via bats tests 41-45)
- [x] CHK-015 Help text matches retained set: `stageman.sh --help` output contains exactly 14 subcommand names

## Edge Cases & Error Handling
<!-- Error states, boundary conditions, failure modes -->
- [x] CHK-016 Internal functions remain callable: `validate_stage`, `get_all_states`, `get_allowed_states`, `validate_stage_state`, `get_next_stage`, `validate_state` are still used internally by `set_stage_state`, `transition_stages`, `validate_status_file`
- [x] CHK-017 Test suite passes: All 53 remaining bats tests pass (exit 0)
- [x] CHK-018 Simple test passes: `test-simple.sh` exits 0

## Code Quality
<!-- Baseline items (no code_quality config) -->
- [x] CHK-019 Pattern consistency: Removals are clean — no orphaned comments, section headers without content, or empty code blocks
- [x] CHK-020 No unnecessary duplication: No accidental duplication introduced during help text rewrite

## Documentation Accuracy
<!-- Project-specific: from config.yaml extra_categories -->
- [x] CHK-021 SPEC-stageman.md matches implementation: API tables list only retained subcommands; no removed subcommand appears
- [x] CHK-022 SPEC-stageman.md changelog: v3.0.0 entry documents the consolidation
- [x] CHK-023 kit-architecture.md: Subcommand count and category listing match actual CLI

## Cross References
<!-- Project-specific: from config.yaml extra_categories -->
- [x] CHK-024 Test-to-implementation alignment: Every retained bats test exercises a subcommand that exists in the dispatch block
- [x] CHK-025 Removed test coverage: No bats test references a removed subcommand

## Notes

- Check items as you review: `- [x]`
- All items must pass before `/fab-continue` (hydrate)
- If an item is not applicable, mark checked and prefix with **N/A**: `- [x] CHK-008 **N/A**: {reason}`
