# Quality Checklist: Operator Spawn Add Fab Sync

**Change**: 260405-xh08-operator-spawn-add-fab-sync
**Generated**: 2026-04-06
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-continue or /fab-ff at the tasks stage.

  This checklist validates that the IMPLEMENTATION matches the SPEC.
  Items are derived from:
    1. spec.md — every requirement should have coverage
    2. Plan decisions — technical choices should be reflected in code
    3. Project constitution — project-wide quality standards

  Categories below are defaults. Add project-specific categories from config.yaml.
  /fab-continue (review) checks all items. ALL must pass before archiving.
-->

## Functional Completeness
<!-- Every requirement in spec.md has working implementation -->
- [ ] CHK-001 Default init script updated: `InitScriptPath()` in `src/go/wt/internal/worktree/context.go` returns `"fab sync"` (not `"fab-kit sync"`) when `WORKTREE_INIT_SCRIPT` is unset
- [ ] CHK-002 Test assertion updated: `TestInitScriptPath_Default` in `src/go/wt/internal/worktree/context_test.go` asserts `"fab sync"` instead of `"fab-kit sync"`
- [ ] CHK-003 Reuse init call added: `src/go/wt/cmd/create.go` `--reuse` collision block calls `RunWorktreeSetup` on `existingWtPath` before `return nil`, gated on `worktreeInit == "true"`
- [ ] CHK-004 New test exists: `TestCreate_ReuseRunsInitScript` is present in `src/go/wt/cmd/create_test.go` and verifies that the init script runs when `--reuse` finds an existing worktree

## Behavioral Correctness
<!-- Changed requirements behave as specified, not as before -->
- [ ] CHK-005 Init call uses `"force"` mode: `RunWorktreeSetup` in the `--reuse` block is called with `"force"` (not `""` / prompt mode), consistent with the non-interactive fresh-create path
- [ ] CHK-006 `--worktree-init false` suppresses reuse init: when `--worktree-init false` is passed, the new init call in the `--reuse` block is skipped (gate `worktreeInit == "true"` is respected)
- [ ] CHK-007 Non-reuse path unchanged: the init block at line 222 (fresh create) is unmodified; `--worktree-init false` on a fresh create still skips init as before
- [ ] CHK-008 Stdout path output preserved: `--reuse` still prints the worktree path to stdout; the replacement `fmt.Println(existingWtPath)` produces the same output as the previous `fmt.Println(filepath.Join(ctx.WorktreesDir, finalName))`

## Removal Verification
<!-- Every deprecated requirement is actually gone -->
- [ ] CHK-009 Stale default removed: no occurrence of `"fab-kit sync"` remains as the fallback return value in `InitScriptPath()` in `context.go`
- [ ] CHK-010 Old test assertion gone: `TestInitScriptPath_Default` no longer references or asserts `"fab-kit sync"`

## Scenario Coverage
<!-- Key scenarios from spec.md have been exercised -->
- [ ] CHK-011 Scenario "Default returned when env var absent": `TestInitScriptPath_Default` passes with `WORKTREE_INIT_SCRIPT=""` asserting `"fab sync"`
- [ ] CHK-012 Scenario "Custom script respected when env var set": `TestInitScriptPath_Custom` (or equivalent) still passes — custom `WORKTREE_INIT_SCRIPT` is returned unchanged
- [ ] CHK-013 Scenario "Init runs on reuse when `--worktree-init` is default": `TestCreate_ReuseRunsInitScript` passes — `.init-script-ran` is present in the reused worktree after `wt create --non-interactive --reuse`
- [ ] CHK-014 Scenario "Init skipped on reuse when `--worktree-init false`": verified (via `TestCreate_ReuseRunsInitScript` variant or `TestCreate_ReuseExisting`) that `.init-script-ran` is absent when `--worktree-init false` is passed
- [ ] CHK-015 Scenario "No init regression on fresh create": `TestCreate_InitScriptRuns` still passes — non-reuse init path is unaffected
- [ ] CHK-016 Scenario "Test validates reuse init": `TestCreate_ReuseExisting` still passes (stdout path unchanged)

## Edge Cases & Error Handling
<!-- Error states, boundary conditions, failure modes -->
- [ ] CHK-017 Init failure on `--reuse` is non-fatal: the error from `RunWorktreeSetup` is discarded with `_ =`; the command still exits 0 and prints the worktree path even when the init script fails
- [ ] CHK-018 Init failure scenario passes: when `WORKTREE_INIT_SCRIPT` points to a command not on PATH, `wt create --non-interactive --reuse` exits 0 and prints the path (consistent with `RunWorktreeSetup` silently skipping unavailable commands)

## Code Quality
<!-- Always included. Expanded from fab/project/code-quality.md -->
- [ ] CHK-019 Pattern consistency: new code in `create.go` follows the same naming and structural patterns as the surrounding `--reuse` block and the existing non-reuse init call (variable naming, error discard style, comment convention)
- [ ] CHK-020 No unnecessary duplication: `InitScriptPath()` and `RunWorktreeSetup` are reused from the `wt` package rather than reimplementing init logic inline in the `--reuse` block
- [ ] CHK-021 Readability: inserted code in `create.go` is clear and maintainable — no clever one-liners; the non-fatal intent is explained by an inline comment
- [ ] CHK-022 No magic strings: `"force"` mode and `"true"` gate in the reuse block are consistent with their usage elsewhere in `create.go`; no new unexplained literals introduced

## Documentation Accuracy
<!-- Extra category from fab/project/config.yaml -->
- [ ] CHK-023 Memory update deferred correctly: the `docs/memory/fab-workflow/kit-architecture.md` update is intentionally omitted from the apply stage (hydrate-stage artifact per spec); no memory file is prematurely modified during apply

## Cross References
<!-- Extra category from fab/project/config.yaml -->
- [ ] CHK-024 Spec–implementation alignment: the code changes in `context.go`, `create.go`, and the two test files match the exact code blocks and line-number references given in `spec.md` and `intake.md`
- [ ] CHK-025 Constitution compliance: changes to the `fab` CLI (Go binary) include corresponding test updates (`context_test.go`, `create_test.go`) per the constitution constraint; no skill files (`src/kit/skills/`) were modified without updating their corresponding spec

## Notes

- Check items as you review: `- [x]`
- All items must pass before `/fab-continue` (hydrate)
- If an item is not applicable, mark checked and prefix with **N/A**: `- [x] CHK-NNN **N/A**: {reason}`
